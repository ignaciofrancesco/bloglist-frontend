import { GlobalStyles } from "@mui/system";

const GlobalStyle = () => (
  <GlobalStyles
    styles={{
      ul: { listStyle: "none", padding: 0, margin: 0 },
    }}
  />
);

export default GlobalStyle;
